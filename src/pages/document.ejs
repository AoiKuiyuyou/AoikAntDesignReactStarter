<!-- # +++++ 9S4Z3 +++++ -->
<!-- # This file is required by Umi. -->
<!DOCTYPE html>
<html lang="zh-CN" manifest="ignore.manifest">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
  <meta http-equiv="Pragma" content="no-cache" />
  <meta http-equiv="Expires" content="0" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0"
  />
  <title></title>
  <style>
    html,
    body,
    #root {
      min-height: 100%;
      margin: 0;
      padding: 0;
    }
  </style>
  <script>
    (function() {
      //
      window.__REACT_DEVTOOLS_GLOBAL_HOOK__ = {
        supportsFiber: true,
        inject: function() {},
        onCommitFiberRoot: function() {},
        onCommitFiberUnmount: function() {},
      };
      //
      var consoleWarnOld = console.warn;
      var warnPatternsIgnored = [
        'has been renamed, and is not recommended for use',
        'react-error-overlay is not meant for use in production. You should ensure it is not included in your build to reduce bundle size.',
      ];
      console.warn = function() {
        if (!arguments.length) {
          return;
        }
        var firstArg = arguments[0];
        for (var i = 0; i < warnPatternsIgnored.length; i += 1) {
          var patternIgnored = warnPatternsIgnored[i];
          if (firstArg.indexOf(patternIgnored) !== -1) {
            return;
          }
        }
        consoleWarnOld.apply(null, arguments);
      };
      //
      var consoleErrorOld = console.error;
      var errorPatternsIgnored = [
        '[React Intl] Missing message',
        '[React Intl] Cannot format message',
      ];
      console.error = function() {
        if (!arguments.length) {
          return;
        }
        var firstArg = arguments[0];
        for (var i = 0; i < errorPatternsIgnored.length; i += 1) {
          var patternIgnored = errorPatternsIgnored[i];
          if (firstArg.indexOf(patternIgnored) !== -1) {
            return;
          }
        }
        consoleErrorOld.apply(null, arguments);
      };
    })();
  </script>
</head>
<body>
<!--[if lte IE 9]>
<div style="font-size: 25px; color: red;">Error：browser unsupported.</div>
<![endif]-->
<script>
  if (!!navigator.userAgent.match(/MSIE 10/)) {
    var errMsg = "Error：browser unsupported.";
    window.alert(errMsg);
    document.execCommand('Stop');
  }
</script>
<!--[if gt IE 9]>
    <%# ----- 1T6U5 ----- %>
    <div id="root" />
    <![endif]-->
</body>
</html>
